# Alembic configuration for the PE service.

[alembic]
# path to migration scripts, relative to this ini file.
script_location = %(here)s/alembic

# sys.path entry so "app" is importable when alembic runs.
prepend_sys_path = .

path_separator = os

# Placeholder only — env.py overrides this with the app's DATABASE_URL.
sqlalchemy.url = driver://user:pass@localhost/dbname


[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
